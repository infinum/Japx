platform :ios, '9.0'
use_frameworks!

target 'Jade_Example' do
  pod 'Jade/RxCodableAlamofire', :path => '../'

  target 'Jade_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 1.2.0'
    pod 'Nimble', '~> 7.0.2'
  end
end
